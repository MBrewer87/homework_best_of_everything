<div class="page-header">
  <h1>User #<%= @user.id %></h1>
</div>

<dl class="dl-horizontal">
  <dt>Username</dt>
  <dd><%= @user.username.capitalize %></dd>

  <p> </p>

  <dt> Favorite Dishes </dt>
  <dd>
      <% @user_favorites.each do |dish| %>
        <li> <%= Dish.find_by({:id => dish.dish_id}).name %> (from <%= Venue.find_by({:id => dish.venue_id}).name %>)</li>
      <% end %>

  <p> </p>

  <dt>

</dl>
